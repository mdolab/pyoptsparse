trigger:
- master

pr:
- master

resources:
  repositories:
  - repository: azure_template
    type: github
    name: mdolab/.github
    endpoint: mdolab

extends:
  template: azure/azure_template.yaml@azure_template
  parameters:
    REPO_NAME: pyoptsparse
    #PRE_BUILD: pre_build.sh
    BUILD_REAL: .github/build.sh
    TEST_REAL: .github/test.sh
    IMAGE: auto
